/* FILE       : kempe.h
   WRITTEN BY : Craig Morgenstern
*/
     
int	Kempe(/* v, chi, maxIC, cssum, numchains,
                      fadj, nadj, scolor, ucolor, color, incon */);
     
     /*
        int    v, chi, maxIC, *cssum, *numchains;
        PFI    fadj, nadj, color, incon;
        PFV    scolor, ucolor;
     */
